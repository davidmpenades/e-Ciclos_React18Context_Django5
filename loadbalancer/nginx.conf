events {}

http {
    upstream backend {
        server backend_container:8000;
    }

    upstream frontend {
        server frontend_react:5173;
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://backend;
        }

        location / {
            proxy_pass http://frontend;
        }
    }
}
